machine:
  python:
    version: 3.4.3

dependencies:
  pre:
    - pip install -U pip wheel setuptools
    - pip install ".[dev]"

test:
    override:
      - coverage run --concurrency=eventlet --source nameko_entrypoint_logger -m pytest tests
      - coverage html -d $CIRCLE_ARTIFACTS --fail-under 100
